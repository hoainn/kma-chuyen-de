.DS_Store
__pycache__/
*.pyc
.venv/

# Large datasets (download separately)
DongTing/

# Local backups
DeSFAM/model.full_trace_backup/
DeSFAM/model_backups/

# Logs
*.log

# Claude Prism local app state (history repo + build artifacts)
.claudeprism/
.prism/

# Full DongTing release (7GB, download separately)
DongTing_Official/
# Downloaded reference PDFs (copyright; keep manifest only)
Docs/*.pdf

# Experiment data + intermediate outputs (reproducible via the scripts)
Experiment/data/
Experiment/output/

# Built PDFs — LaTeX/Markdown sources are committed; PDFs are build artifacts
*.pdf

# Outdated material set aside (~243MB: binaries + a nested git repo); do not track
archive/

# Downloaded reference PDFs are copyright (also covered by *.pdf above)
Reference/
